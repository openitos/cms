<h1>/Role</h1>

<p>
  <b>Name</b><br>
  <%= @role.name %>
</p>

<div class="form-actions">
  <%= link_to 'Back', roles_path, :class => 'btn'  %>
  <%= link_to 'Edit', edit_role_path(@role), :class => 'btn' %>
  <%= link_to 'Delete', role_path(@role), :method => 'delete', :confirm => 'Are you sure?', :class => 'btn btn-danger' %>
</div>
